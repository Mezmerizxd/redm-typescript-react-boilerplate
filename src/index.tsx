// Modules
import React from 'react';
import ReactDOM from 'react-dom';

import Spawn from "./nui/Example";

ReactDOM.render(
  <React.StrictMode>
    <Spawn />
  </React.StrictMode>,
  document.getElementById('root')
);